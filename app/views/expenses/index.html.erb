<div class="container mt-10">
  <h1>My expenses</h1>
  <ul id="expenses">
    <% @expenses.each do |e| %>
      <li>
        <%=  e.name %>
        <%=  e.amount_spent %>
      </li>
    <% end %>
  </ul>
  <%= pie_chart @expenses.group(:name ).count, donut: true %>


</div>
